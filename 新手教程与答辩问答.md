# STM32 语音对话上位机：新手教程与答辩问答

## 一、这个项目能做什么

本项目把 STM32、电脑麦克风、云端大模型和 LCD 屏幕连接起来，实现以下功能：

- 在电脑上输入文字并获得回答。
- 按住开发板 KEY0 说话，松开后自动识别并回答。
- 点击“电脑麦克风”按钮进行语音问答。
- 查询当前日期、时间和天气。
- 识别“前进、后退、左转、右转、停止”等动作指令。
- 将问题、回答和方向箭头转换成黑白位图，通过串口发送到 STM32 屏幕。

当前版本主要演示“语音交互、动作识别和屏幕显示”。方向指令会显示箭头，但没有接入电机驱动，因此不会让小车实际运动。

## 二、系统工作流程

```text
KEY0 / 电脑麦克风 / 文字输入
              ↓
电脑获取语音并转换成文字
              ↓
时间和天气问题 → 本地专用处理
其他问题       → GLM-4-Flash 大模型
              ↓
得到回答文字和动作类型
              ↓
Pillow 生成 1 位黑白位图
              ↓
串口 115200 发送给 STM32
              ↓
STM32 LCD 显示问答和方向箭头
```

## 三、主要文件说明

| 文件 | 作用 |
|---|---|
| `qwen_chat_gui_final.py` | 图形界面、文字输入、屏幕预览和主流程 |
| `qwen_pc_demo.py` | 大模型请求、语音识别、串口通信和位图发送 |
| `local_queries.py` | 日期、时间和实时天气查询 |
| `local_api_config.py` | 本机 API 配置，请勿公开 |
| `requirements.txt` | Python 依赖列表 |
| `install_pc.bat` | 自动检查 Python 并安装依赖 |
| `run_pc_demo.bat` | 启动上位机 |
| `实验X 语音动作显示实验` | STM32 Keil 工程 |

虽然文件夹名称保留了 `qwen`，当前代码实际调用的是智谱 `GLM-4-Flash`。这是项目开发过程中保留下来的历史命名。

## 四、第一次使用

### 1. 准备工作

需要准备：

- Windows 电脑。
- Python 3.10 或更高版本。
- 可联网环境。
- 电脑麦克风。
- 已烧录程序的 STM32F407 开发板和 LCD。
- USB 转串口连接线。

如果使用外置 USB 转串口模块，应将 TX 和 RX 交叉连接，并连接 GND。串口电平应与开发板匹配，不要随意接入 5V。

### 2. 安装电脑端环境

进入 `qwen_pc_demo` 文件夹，双击：

```text
install_pc.bat
```

脚本会安装：

- `pyserial`：串口通信。
- `Pillow`：生成 LCD 位图。
- `SpeechRecognition`：语音识别。
- `PyAudio`：麦克风录音。

安装完成后不要删除项目中的 Python 文件。

### 3. 编译和烧录 STM32

用 Keil 打开：

```text
实验X 语音动作显示实验\Projects\MDK-ARM\atk_f407.uvprojx
```

执行 `Project → Rebuild all target files`，然后把生成的 HEX 文件烧录到开发板：

```text
实验X 语音动作显示实验\Output\atk_f407.hex
```

烧录完成后复位开发板。

### 4. 连接开发板

1. 用 USB 转串口连接开发板和电脑。
2. 关闭串口助手，避免 COM 端口被占用。
3. 打开设备管理器，确认串口设备已经出现。
4. 双击 `run_pc_demo.bat`。
5. 界面显示“已连接 COMx，等待 KEY0”即表示连接成功。

如需查看全部串口，可以在 PowerShell 中执行：

```powershell
python -m serial.tools.list_ports -v
```

## 五、基本操作

### 1. 文字问答

在窗口底部输入问题，点击“发送文字”或按回车。

建议依次测试：

```text
1加1等于几？
今天是几月几日？
今天天气怎么样？
今天北京天气怎么样？
```

天气问题不写城市时，会根据电脑公网 IP 粗略判断位置；答辩演示时写出城市更稳定。

### 2. 电脑麦克风问答

1. 点击“电脑麦克风”。
2. 对着电脑麦克风清楚说话。
3. 等待语音识别和大模型回答。

### 3. 开发板 KEY0 语音问答

1. 按住 KEY0。
2. 说完一句完整的话。
3. 松开 KEY0。
4. 等待识别、回答和屏幕刷新。

录音时间太短或环境噪声太大时，可能提示“没有识别出清晰的语音”。

### 4. 动作指令

| 说法示例 | 动作值 | 屏幕效果 |
|---|---|---|
| 前进、向前走 | `FORWARD` | 向上箭头 |
| 后退、向后走 | `BACK` | 向下箭头 |
| 左转、向左 | `LEFT` | 向左箭头 |
| 右转、向右 | `RIGHT` | 向右箭头 |
| 停止、停下 | `STOP` | 停止提示，无方向箭头 |
| 普通问答 | `NONE` | 只显示文字 |

## 六、推荐的课堂演示顺序

为了减少现场出错，可以按以下顺序演示：

1. 启动软件，说明串口已自动连接。
2. 输入“1加1等于几”，展示普通问答。
3. 输入“今天是几月几日”，展示本地日期处理。
4. 输入“今天北京天气怎么样”，展示实时天气。
5. 输入“前进”“后退”“左转”“右转”，展示动作识别和箭头。
6. 按住 KEY0 说一个简短问题，展示完整语音链路。
7. 最后说明当前版本只显示动作，下一步可接电机驱动。

正式演示前，应关闭所有旧的上位机窗口，只保留一个 `qwen_chat_gui_final.py` 进程，避免旧进程继续使用修改前的代码或占用串口。

## 七、常见问题排查

### 1. 显示“未找到 STM32 串口”

- 检查 USB 线和驱动。
- 拔下再插入开发板，观察新增的 COM 端口。
- 关闭串口助手、Keil 串口窗口等占用端口的软件。
- 确认 USB 转串口模块的 TX、RX 和 GND 接线。

### 2. 可以输入文字，但 KEY0 没反应

- 确认烧录的是当前工程生成的 HEX。
- 检查 KEY0 是否接在固件配置的引脚上。
- 查看上位机是否收到 `@BTN:DOWN` 和 `@BTN:UP`。
- 按住说话，不要只快速点击按键。

### 3. 麦克风无法录音

- 在 Windows 隐私设置中允许桌面应用使用麦克风。
- 确认默认录音设备正确。
- 重新运行 `install_pc.bat`，检查 `PyAudio` 是否安装成功。

### 4. 普通问题没有回答

- 检查电脑网络。
- 检查 API 配置是否有效。
- 查看日志中是否出现 `GLM API HTTP` 或“无法连接 GLM API”。
- 不要同时启动多个上位机窗口。

### 5. 天气无法查询

- 天气数据来自网络，首先检查网络连接。
- 使用“今天北京天气怎么样”这种包含城市的完整问法。
- 不写城市时使用公网 IP 定位，位置可能不够精确。

### 6. LCD 没有显示图片

- 确认波特率为 `115200`。
- 检查 STM32 是否回复 `OK`。
- 图片宽度不能超过 480 像素。
- 一帧位图不能超过 6144 字节。
- 检查 TX/RX 是否交叉连接、GND 是否共地。

### 7. 箭头和文字仍然重叠

- 完全关闭之前启动的所有上位机窗口。
- 在任务管理器中结束残留的 `python.exe`。
- 重新双击当前项目中的 `run_pc_demo.bat`。
- 当前程序会按屏幕高度动态分配文字区和箭头区。

## 八、老师可能提出的问题与参考答案

以下答案可以直接作为答辩参考，但现场最好用自己的语言表达。

### 1. 这个项目的核心功能是什么？

答：核心功能是把语音识别、大模型问答、动作识别和 STM32 屏幕显示组合起来。用户可以通过 KEY0、电脑麦克风或文字输入提出问题，电脑处理后把回答和方向图标发送给 STM32 显示。

### 2. 为什么不直接在 STM32 上运行大模型？

答：STM32F407 的 RAM、Flash 和计算能力有限，无法直接运行这种规模的大模型。电脑负责录音、联网、模型推理和生成位图，STM32负责按键、串口通信和 LCD 显示，这样分工更符合硬件能力。

### 3. STM32 在系统中具体负责什么？

答：STM32负责检测 KEY0 按下和松开、向电脑上报按键事件、报告 LCD 分辨率、接收位图数据并显示到 LCD。目前它不负责语音识别和大模型推理。

### 4. 完整的数据流程是什么？

答：KEY0触发电脑录音，语音先转换成文字；时间和天气问题由专用程序处理，其他问题发送给 GLM；程序得到回答和动作类型后生成 1 位位图，通过串口发送给 STM32，最后显示在 LCD 上。

### 5. 当前使用的是什么大模型？为什么文件名叫 qwen？

答：当前实际调用的是智谱 GLM-4-Flash。`qwen_pc_demo` 是早期开发阶段保留的项目名称，不代表当前模型。正式工程可以进一步统一命名，避免歧义。

### 6. 为什么要求模型返回 JSON？

答：JSON可以把自然语言回答和动作类型分开，例如 `action` 表示动作，`reply` 表示回答。程序可以稳定解析动作，而不需要从一大段自然语言中猜测控制指令。

### 7. 如果模型没有按要求返回 JSON 怎么办？

答：程序有容错解析。严格 JSON 解析失败时，会从文本中识别动作关键词；如果识别到动作，就使用预设的简短确认语，避免把模型的长篇解释直接显示到小屏幕上。

### 8. 普通问答为什么不会误触发动作？

答：动作只允许 `FORWARD、BACK、LEFT、RIGHT、STOP` 五类，普通问题对应 `NONE`。系统提示词和本地动作关键词都对动作范围进行了限制。

### 9. 日期为什么不直接询问大模型？

答：大模型不一定知道电脑当前的真实时间，容易给出过期或错误答案。程序直接读取北京时间，因此日期和时间更准确，也减少一次模型请求。

### 10. 天气数据从哪里来？

答：天气由 `wttr.in` 的网络接口提供。用户写出城市时按城市查询；不写城市时根据公网 IP 粗略定位。因为公网 IP 定位可能有偏差，所以演示时最好明确说出城市。

### 11. 为什么使用串口通信？

答：串口实现简单、调试方便，而且 STM32 和电脑都容易支持。这个项目的数据量不大，115200 波特率可以满足按键事件和黑白位图传输需求。

### 12. 串口波特率为什么是 115200？

答：115200 是常用的高速串口波特率，兼容性较好。相比 9600，它发送一幅几 KB 的图片更快，同时对普通 USB 转串口模块也比较稳定。

### 13. 电脑和 STM32 之间有哪些协议消息？

答：主要有三类：STM32用 `@BTN:DOWN` 和 `@BTN:UP` 上报按键状态，用 `@DEV:宽x高` 上报屏幕尺寸；电脑用 `@IMG` 头加二进制位图发送屏幕内容。STM32处理成功后回复 `OK`，失败时回复 `ERR`。

### 14. 为什么发送位图，而不是直接发送中文字符串？

答：发送位图可以直接使用电脑上的中文字体，避免 STM32 字库不完整、编码不一致或生僻字无法显示的问题。STM32只需要逐像素显示，处理逻辑更简单。

### 15. 为什么使用 1 位黑白位图？

答：1 位位图每个像素只占 1 bit，比 RGB565 每个像素占 16 bit 节省很多内存和串口传输时间，适合 STM32 的内存条件，也足以显示文字和箭头。

### 16. 6144 字节的限制是怎么来的？

答：STM32端为图片准备了 6144 字节缓冲区。1 位位图的数据量约为“宽度乘高度再除以 8”。例如 `320 × 152 ÷ 8 = 6080` 字节，没有超过 6144 字节。

### 17. 如果图片宽度不是 8 的倍数怎么办？

答：每一行都要按完整字节存储，所以每行字节数使用 `(宽度 + 7) // 8` 计算，再乘以高度。这样可以向上取整，不会丢失行尾像素。

### 18. 软件如何适配不同分辨率？

答：STM32启动后通过 `@DEV` 把 LCD 宽高发给电脑。电脑根据真实宽高和 6144 字节上限计算图片尺寸，再动态换行和调整箭头大小。

### 19. 箭头重叠问题是怎样解决的？

答：程序不再使用固定坐标直接覆盖文字，而是先根据屏幕高度预留箭头区域，限制文字行数，再在独立区域内缩放并绘制箭头，因此短屏幕也不会与文字重叠。

### 20. KEY0 为什么使用“按住录音、松开识别”？

答：这种按键说话方式不需要一直监听，能减少误触发，也能明确一段语音的开始和结束。STM32只需上报按下和松开事件，实际音频仍由电脑麦克风采集。

### 21. 语音识别在哪里完成？

答：电脑使用 `SpeechRecognition` 获取音频，并调用 Google 中文语音识别服务转换成文字。STM32本身不传输音频数据。

### 22. 为什么网络断开后不能完整使用？

答：当前语音识别、大模型和天气都依赖网络。串口通信、按键检测、屏幕显示以及电脑本地日期仍可工作。若需要离线运行，可以改成本地语音模型和本地大模型。

### 23. 为什么图形界面不会在请求模型时完全卡死？

答：语音识别和网络请求放在后台线程中执行，主线程继续处理 Tkinter 界面。后台线程把结果放入队列，主线程定时读取队列并更新界面。

### 24. 系统如何保存上下文？

答：大模型客户端保存最近的对话消息，并在请求时发送最近 11 条消息。这样能进行简单的连续对话，同时限制请求长度。

### 25. 系统有哪些误差来源？

答：主要包括环境噪声导致的语音识别错误、大模型回答不稳定、天气接口或公网 IP 定位误差、网络延迟，以及串口接线或端口占用问题。

### 26. 系统中最耗时的步骤是什么？

答：通常是语音识别和云端大模型请求，串口发送几 KB 的黑白位图所占时间相对较短。网络质量会明显影响总响应时间。

### 27. 为什么需要 STM32 回复 OK 或 ERR？

答：电脑需要知道 STM32 是否完整接收并接受了图片。收到 `OK` 才能确认传输成功；超时或收到 `ERR` 时可以在上位机显示错误，方便定位问题。

### 28. API Key 为什么不能公开？

答：API Key相当于服务账号的密码，公开后可能被别人盗用并产生费用。当前演示为了方便保存在本地配置文件中，正式发布时应使用环境变量、密钥管理服务或让用户自行配置，并避免上传到公开仓库。

### 29. 如果要让小车真的运动，还需要做什么？

答：需要增加电机、电机驱动模块和电源，并在 STM32 中把 `FORWARD、BACK、LEFT、RIGHT、STOP` 映射为 GPIO 或 PWM 控制。同时必须加入限速、急停、超时自动停止等安全措施。

### 30. 后续可以怎样改进？

答：可以加入离线语音识别、本地大模型、串口选择界面、语音播报、对话记录保存、电机控制、传感器反馈、CRC校验、断线重连和更完善的密钥管理。

## 九、30 秒项目介绍参考稿

> 本项目是一套基于 STM32 和电脑上位机的语音对话显示系统。STM32负责 KEY0 按键、串口通信和 LCD 显示，电脑负责录音、语音识别、GLM 问答、天气与日期查询，以及位图生成。系统可以识别前进、后退、左转、右转和停止等动作，并把回答和方向箭头通过 115200 波特率的串口发送到 STM32。为了节省内存，屏幕内容采用 1 位黑白位图，每帧不超过 6144 字节。当前版本完成了交互和显示验证，后续可以继续接入电机驱动实现真实运动。

